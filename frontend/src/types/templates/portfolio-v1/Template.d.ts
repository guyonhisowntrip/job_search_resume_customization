import { ComponentType } from "react"

import { ResumeData } from "@/lib/resume-schema"

declare const PortfolioTemplateV1: ComponentType<{ resumeData: ResumeData }>

export default PortfolioTemplateV1
